apiVersion: v1
kind: Service
metadata:
  name: model-service
  labels:
    app: model-service
spec:
  selector:
    app: model-service
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP